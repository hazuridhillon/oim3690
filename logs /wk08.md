learning log 3/10

quiz

questions from code (put into playground.html)

Time Button
. Why does the time only update when I click — what would make it update every second automatically?
Hide & Seek
. Why does the toggle work by adding/removing a CSS class instead of just setting display: none directly in JS?
Surprise Button
. Why does the confirm() popup block everything on the page until I respond?
. Why is this the only button that uses onclick directly in the HTML instead of addEventListener?
